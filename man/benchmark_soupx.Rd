% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{benchmark_soupx}
\alias{benchmark_soupx}
\title{Benchmark SoupX Performance}
\usage{
benchmark_soupx(sc, operations = c("SoupChannel", "autoEstCont", "adjustCounts"), 
    iterations = 5, verbose = TRUE)
}
\arguments{
\item{sc}{A SoupChannel object to benchmark}
\item{operations}{Character vector of operations to benchmark. Options: "SoupChannel", "autoEstCont", "adjustCounts", "expandClusters"}
\item{iterations}{Number of iterations to run for each operation}
\item{verbose}{Whether to print progress messages}
}
\value{
A data.frame with performance metrics for each operation
}
\description{
Benchmarks the performance of various SoupX operations including timing and memory usage.
}
\examples{
# Benchmark core SoupX operations
if(requireNamespace("Matrix", quietly = TRUE)) {
  # Create a small test dataset
  toc <- Matrix::Matrix(rpois(1000, 5), nrow = 100, ncol = 10, sparse = TRUE)
  tod <- Matrix::Matrix(rpois(2000, 3), nrow = 100, ncol = 20, sparse = TRUE)
  rownames(toc) <- rownames(tod) <- paste0("Gene", 1:100)
  colnames(toc) <- paste0("Cell", 1:10)
  colnames(tod) <- paste0("Droplet", 1:20)
  
  sc <- SoupChannel(tod, toc)
  results <- benchmark_soupx(sc, iterations = 2, verbose = FALSE)
  print(results)
}
} 