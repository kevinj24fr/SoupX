% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotFunctions.R
\name{plotContaminationByCluster}
\alias{plotContaminationByCluster}
\title{Plot Contamination Fraction by Cluster}
\usage{
plotContaminationByCluster(sc, clusters = NULL, plotType = "boxplot")
}
\arguments{
\item{sc}{A SoupChannel object with clustering information}
\item{clusters}{Optional cluster vector if not in sc$metaData}
\item{plotType}{Type of plot: "boxplot", "violin", or "bar"}
}
\value{
A ggplot2 object
}
\description{
Visualizes contamination fraction estimates across different cell clusters to identify 
potential cluster-specific contamination patterns.
}
\examples{
# Plot contamination by cluster
if(requireNamespace("ggplot2", quietly = TRUE)) {
  # Use the toy dataset
  data(scToy)
  
  # Set some dummy clusters
  scToy$metaData$clusters <- rep(c("A", "B"), each = nrow(scToy$metaData)/2)
  
  # Plot contamination by cluster
  gg <- plotContaminationByCluster(scToy)
  print(gg)
}
} 